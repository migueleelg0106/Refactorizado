<UserControl x:Class="PictionaryMusicalCliente.Vista.Chat"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:language="clr-namespace:PictionaryMusicalCliente.Properties.Langs"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="320">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="LightYellow" BorderBrush="#FFD7D7A8" BorderThickness="1">
            <TextBlock Text="{x:Static language:Lang.partidaTextoChat}" FontWeight="Bold" 
                       FontSize="16" VerticalAlignment="Center" Margin="8,0,0,0" 
                       FontFamily="Comic Sans MS"/>
        </Border>

        <ScrollViewer x:Name="desplazamientoChat" Grid.Row="1" Background="White" BorderBrush="Black" BorderThickness="1">
            <StackPanel x:Name="panelApilableChat" Margin="6"/>
        </ScrollViewer>

        <TextBox x:Name="campoTextoChat" Grid.Row="2" Background="LightYellow" BorderBrush="Black" 
                 BorderThickness="1"
                 Text="{Binding DataContext.MensajeChat, RelativeSource={RelativeSource AncestorType=Window}, UpdateSourceTrigger=PropertyChanged}"
                 IsEnabled="{Binding PuedeEscribir}" KeyDown="AlPresionarTeclaEnCampoTextoChat"
                 MaxLength="150"/>
    </Grid>
</UserControl>
