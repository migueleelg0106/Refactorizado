<Window x:Class="PictionaryMusicalCliente.Vista.InvitarAmigos"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:language="clr-namespace:PictionaryMusicalCliente.Properties.Langs"
        Title="{x:Static language:Lang.invitarAmigosTextoInvitarAmigos}"
        Height="420"
        Width="420"
        Background="Wheat"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Border Background="White" BorderBrush="Black" BorderThickness="2" CornerRadius="6">
            <DockPanel Margin="12">
                <TextBlock Text="{x:Static language:Lang.invitarAmigosTextoInvitarAmigos}"
                           DockPanel.Dock="Top" FontSize="20" FontWeight="Bold"
                           FontFamily="Comic Sans MS" HorizontalAlignment="Center"
                           Margin="0,0,0,12"/>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Amigos}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderThickness="0,0,0,1" BorderBrush="#FFE0E0E0" 
                                        Padding="6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="{Binding NombreUsuario}"
                                                   VerticalAlignment="Center"
                                                   FontFamily="Comic Sans MS"
                                                   FontSize="16"/>

                                        <Button Grid.Column="1"
                                                Content="{Binding TextoBoton}"
                                                Command="{Binding InvitarComando}"
                                                Margin="16,0,0,0" Padding="14,6"
                                                FontFamily="Comic Sans MS"
                                                FontWeight="Bold" Cursor="Hand">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background" Value="LightGreen"/>
                                                    <Setter Property="Foreground" Value="Black"/>
                                                    <Setter Property="BorderBrush" Value="DarkGreen"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding InvitacionEnviada}" Value="True">
                                                            <Setter Property="Background" Value="DarkGreen"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding EstaProcesando}" Value="True">
                                                            <Setter Property="IsEnabled" Value="False"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </DockPanel>
        </Border>
    </Grid>
</Window>